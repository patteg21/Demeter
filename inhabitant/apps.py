from django.apps import AppConfig


class InhabitantConfig(AppConfig):
    default_auto_field = "django.db.models.BigAutoField"
    name = "inhabitant"

    # def ready(self) -> None:
    #    import
